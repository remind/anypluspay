project:
  name: payment
  author: wxj

db:
  dataSource:
    url: jdbc:mysql://127.0.0.1:3306/ap_payment?useUnicode=true&characterEncoding=utf8
    user: root
    password: root
  table:
    prefix:
      - tp_
      - t_
    name:
    # 依次为：表名,业务主键字段(如果也是表主键就不用)
#      - tp_payment,payment_id
#      - tp_fund_detail,detail_id
      - tp_flux_order
#      - tp_flux_instruction
#      - tp_refund_order
      - tp_general_pay_order
#      - t_withdraw_order